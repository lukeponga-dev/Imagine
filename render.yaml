# This file describes how your application is built and deployed on Render.com

services:
  - type: static_site   # ✅ Correct type for static frontend apps
    name: image-enhancer-ai
    buildCommand: npm install && npm run build
    publishPath: dist

    # Optional environment variables (set in Render dashboard)
    # envVars:
    #   - key: API_KEY
    #     value: your-api-key-here

    # Optional rewrites for client-side routing (React Router, etc.)
    # rewrites:
    #   - source: /*
    #     destination: /index.html
